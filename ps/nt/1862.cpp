/*
 * Problem:
 * https://www.lintcode.com/problem/1862/?_from=problem_tag&fromId=undefined
 * Tags: DFS
 */

#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <iomanip>
#include <iostream>
#include <iterator>
#include <list>
#include <map>
#include <numeric>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <tuple>
#include <unordered_map>
#include <unordered_set>
#include <vector>
#define begend(v) ((v).begin()), ((v).end())
#define sz(v) ((int)((v).size()))
using namespace std;

class Solution {
public:
  /**
   * @param father: the father of every node
   * @param time: the time from father[i] to node i
   * @return: time to flower tree
   */
  int timeToFlowerTree(vector<int> &father, vector<int> &time) { return 0; }
};

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
}
